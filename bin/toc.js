// Generated by LiveScript 1.6.0
(function(){
  var build, join$ = [].join, out$ = typeof exports != 'undefined' && exports || this;
  build = function(y){
    var hsh, makeOl;
    hsh = y.hsh;
    log(hsh);
    makeOl = compose$(values, sortBy(path(['$', 'ord'])), map(desc), (function(it){
      return join$.call(it, '');
    }));
    function desc(x){
      var li, ol;
      li = (function(){
        var ref$;
        switch (false) {
        case !!((ref$ = x.$) != null && ref$.key):
          return '';
        case hsh !== x.$.hsh:
          return "<li class=current-item>" + x.$.key + "\n";
        default:
          return "<li><a href='/" + x.$.lnk + "'>" + x.$.key + "</a>\n";
        }
      }());
      ol = x._ ? "\n<ol>" + makeOl(x._) + "</ol>\n" : '';
      return li + ol;
    }
    return desc(y);
  };
  out$.build = build;
  function compose$() {
    var functions = arguments;
    return function() {
      var i, result;
      result = functions[0].apply(this, arguments);
      for (i = 1; i < functions.length; ++i) {
        result = functions[i](result);
      }
      return result;
    };
  }
}).call(this);
